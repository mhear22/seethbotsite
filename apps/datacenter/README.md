# Data Center App

Standalone Data Center minigame frontend app.

- Source: `apps/datacenter/frontend`
- Canonical route: `/datacenter/`
- Build output: `backend/datacenter-webdist`

Run locally:

```bash
cd apps/datacenter/frontend
npm install
npm run dev
```
