# NPM configuration to reduce memory usage during install
maxsockets=1
prefer-offline=true
audit=false
# pnpm: hoist phantom deps (npm-like) so TS can resolve transitive @types portably
shamefully-hoist=true
